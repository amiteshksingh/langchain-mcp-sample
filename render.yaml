services:

  - type: web

    name: kyc-agent-demo

    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      uvicorn app:app --host 0.0.0.0 --port 10000

    plan: free